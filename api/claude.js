const { json, readBody, checkOrigin } = require('./_helpers');
module.exports=async(req,res)=>{ if(!checkOrigin(req)) return json(res,403,{error:'Origine non autorisée.'}); if(req.method!=='POST') return json(res,405,{error:'POST requis.'}); try{ if(!process.env.ANTHROPIC_API_KEY) return json(res,500,{error:'ANTHROPIC_API_KEY manquant côté serveur.'}); const {prompt,withWeb=true}=await readBody(req); if(!prompt) return json(res,400,{error:'Prompt manquant.'}); const bodyBase={model:'claude-haiku-4-5',max_tokens:2800,system:'Tu es un consultant senior data & média spécialisé YouTube, sport, télévision et plateformes numériques. Tu ne dois pas inventer de chiffres. Quand tu fais une hypothèse, tu dois clairement la qualifier.',messages:[{role:'user',content:prompt}]}; const body=withWeb?{...bodyBase,tools:[{type:'web_search_20250305',name:'web_search',max_uses:5,user_location:{type:'approximate',city:'Paris',region:'Île-de-France',country:'FR',timezone:'Europe/Paris'}}]}:bodyBase; const r=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'x-api-key':process.env.ANTHROPIC_API_KEY,'anthropic-version':'2023-06-01','content-type':'application/json'},body:JSON.stringify(body)}); const data=await r.json().catch(()=>({})); json(res,r.ok?200:r.status,data); }catch(e){json(res,500,{error:e.message});} };
