module.exports=async(req,res)=>{ const code=req.query.code; const origin=`https://${req.headers.host}`; const redirect_uri=`${origin}/api/oauth/google/callback`; if(!code){res.statusCode=400;return res.end('Code OAuth manquant.');} const body=new URLSearchParams({code,client_id:process.env.GOOGLE_CLIENT_ID||'',client_secret:process.env.GOOGLE_CLIENT_SECRET||'',redirect_uri,grant_type:'authorization_code'}); const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body}); const data=await r.json().catch(()=>({})); res.setHeader('content-type','text/html; charset=utf-8'); if(!r.ok) return res.end(`<h1>Erreur OAuth</h1><pre>${JSON.stringify(data,null,2)}</pre><p>Vérifie GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET et l'Authorized redirect URI : ${redirect_uri}</p>`); res.end(`<!DOCTYPE html><meta charset="utf-8"><title>OAuth OK</title><body style="font-family:Arial;padding:30px;background:#f6f7fb"><h1>Connexion YouTube Analytics réussie</h1><p>Copie le refresh token ci-dessous dans Vercel → Environment Variables → <b>YOUTUBE_REFRESH_TOKEN</b>, puis redéploie.</p><textarea style="width:100%;height:120px">${data.refresh_token||''}</textarea><pre>${JSON.stringify({scope:data.scope,expires_in:data.expires_in,token_type:data.token_type},null,2)}</pre><p><a href="/admin">Retour admin</a></p></body>`); };
