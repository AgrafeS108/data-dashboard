module.exports=async(req,res)=>{ const origin=`https://${req.headers.host}`; const redirect_uri=`${origin}/api/oauth/google/callback`; const scope='https://www.googleapis.com/auth/yt-analytics.readonly https://www.googleapis.com/auth/youtube.readonly'; const params=new URLSearchParams({client_id:process.env.GOOGLE_CLIENT_ID||'',redirect_uri,response_type:'code',access_type:'offline',prompt:'consent',scope}); res.statusCode=302; res.setHeader('location',`https://accounts.google.com/o/oauth2/v2/auth?${params.toString()}`); res.end(); };
