const { json, checkOrigin } = require('./_helpers');
module.exports=async(req,res)=>{ if(!checkOrigin(req)) return json(res,403,{error:'Origine non autorisée.'}); try{ const endpoint=req.query.endpoint; if(!endpoint||!['channels','playlistItems','videos','search','commentThreads','playlists'].includes(endpoint)) return json(res,400,{error:'Endpoint YouTube non autorisé.'}); if(!process.env.YOUTUBE_API_KEY) return json(res,500,{error:'YOUTUBE_API_KEY manquant côté serveur.'}); const params=new URLSearchParams(); for(const [k,v] of Object.entries(req.query)){ if(k==='endpoint'||k==='key') continue; if(Array.isArray(v)) params.set(k,v[v.length-1]); else if(v!=null) params.set(k,v); } params.set('key',process.env.YOUTUBE_API_KEY); const r=await fetch(`https://www.googleapis.com/youtube/v3/${endpoint}?${params.toString()}`); const data=await r.json().catch(()=>({})); json(res,r.ok?200:r.status,data); }catch(e){json(res,500,{error:e.message});} };
